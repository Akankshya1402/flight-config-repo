server:
  port: 8083

spring:
  application:
    name: notification-service

  config:
    import: "optional:configserver:http://localhost:8888"

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  mail:
    host: smtp.gmail.com
    port: 587
    username: akankshyap1402@gmail.com
    password: pthy wsur endm lydd
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  kafka:
    consumer:
      group-id: notification-service-group
      auto-offset-reset: earliest

# Same topic name used by BookingEventProducer (@Value("${kafka.topics.booking-created}"))
kafka:
  topics:
    booking-created: booking.created

booking:
  email:
    exchange: booking.exchange
    routing-key: booking.email
    queue: booking.email.queue

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"

